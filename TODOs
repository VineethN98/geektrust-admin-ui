- replace loading with a spinner
- Add basic tests like component is visible
- Try and move the url to a .env file
- Change a href to div
- Hanlde case in Table component if no user is present
- Separate out the functions in AdminPage in utils folder
- Remove aqua color from AdminPage css
- Change Fragment with tr and remove it from Row and Edit component
- Remove logs
- use UseRef if required
- Add redux
- Save edited user when enter is pressed

  Table.js
  // <tr key={user.id}>
  // <td>{user.name}</td>
  // <td>{user.email}</td>
  // <td>{user.role}</td>
  // <td>
  // <FaEdit className="editButton" />{" "}
  // <FaTrashAlt
  // className="deleteButton"
  // onClick={() => deleteUser(user.id)}
  // />
  // </td>
  // </tr>

Table.js
// const newUsers = [...users];
// const indexOfUser = users.findIndex((user) => user.id === editRowId);

// newUsers[indexOfUser] = editedUser;
// setUsers(newUsers);
// setEditRowId(null);
